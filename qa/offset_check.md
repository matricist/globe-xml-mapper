# Off-by-one 행 오프셋 자동 점검

각 매퍼의 `ws.Cell(row_expr, col)` 호출에서 해결된 행 위치가 **template 의 라벨 셀** 에 해당하면 보고.
라벨 셀을 값으로 읽고 있으면 거의 확실한 off-by-one 버그.

> 한계: b1/b2 는 첫 블록 기준으로만 해석. FindRow 는 템플릿의 해당 헤더 첫 출현으로 해석.
> 단일 변수(변수명만 있는 `ws.Cell(r, col)`) 는 +0 오프셋으로 간주.

## ✓ 의심 사례 없음

